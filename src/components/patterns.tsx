"use client";
import React, { useState } from "react";

type Props = {
  patterns: {
    attributes: { titolo: string; descrizione: string };
  }[];
};

const Patterns = ({ patterns }: Props) => {
  // const [patterns, setPatterns] = useState<
  //
  // >([]);
  return <div>Patterns</div>;
};

export default Patterns;
